[[naming]]
== ISA 扩展命名约定

本章描述了 RISC-V ISA 扩展命名方案，该方案用于简洁地描述硬件实现中存在的指令集，或应用程序二进制接口 (ABI) 使用的指令集。

[NOTE]
====
RISC-V ISA 旨在支持各种具有各种实验性指令集扩展的实现。我们发现有组织的命名方案简化了软件工具和文档。
====

=== 大小写敏感性

ISA 命名字符串不区分大小写。

=== 基本整数 ISA

RISC-V ISA 字符串以 RV32I、RV32E、RV64I 或 RV64E 开头，表示基本整数 ISA 支持的地址空间大小（以位为单位）。

=== 指令集扩展名称

标准 ISA 扩展被赋予一个由单个字母组成的名称。例如，整数基准的前四个标准扩展是："M"表示整数乘法和除法，"A"表示原子内存指令，"F"表示单精度浮点指令，"D"表示双精度浮点指令。任何 RISC-V 指令集变体都可以通过将基本整数前缀与包含的扩展名称连接起来简洁地描述，例如"RV64IMAFD"。

我们还定义了缩写"G"来表示"IMAFDZicsr_Zifencei"基准和扩展，因为这旨在表示我们的标准通用 ISA。

RISC-V ISA 的标准扩展被赋予其他保留字母，例如，"Q"表示四精度浮点，"C"表示 16 位压缩指令格式。

一些 ISA 扩展依赖于其他扩展的存在，例如，"D"依赖于"F"，"F"依赖于"Zicsr"。这些依赖关系可能在 ISA 名称中是隐式的：例如，RV32IF 等效于 RV32IFZicsr，RV32ID 等效于 RV32IFD 和 RV32IFDZicsr。

=== 下划线

下划线"_"可用于分隔 ISA 扩展，以提高可读性和提供消歧，例如"RV32I2_M2_A2"。

=== 其他标准非特权扩展名称

标准非特权扩展也可以通过使用单个"Z"后跟字母数字名称来命名。名称必须以字母字符结尾。如果最后一个字母是"p"，则倒数第二个字母不能是数字。例如，"Zifencei"命名了 <<zifencei>> 中描述的指令获取栅栏扩展。

"Z"后面的第一个字母通常表示最密切相关的字母扩展类别，IMAFDQLCBKJTPVH。例如，用于附加浮点指令的"Zfa"扩展，字母"f"表示该扩展与"F"标准扩展相关。如果命名了多个"Z"扩展，它们应首先按类别排序，然后在类别内按字母顺序排序——例如，"Zicsr_Zifencei_Ztso"。

所有多字母扩展，包括带有"Z"前缀的扩展，必须用下划线与其他多字母扩展分隔，例如"RV32IMACZicsr_Zifencei"。

=== Supervisor 级指令集扩展名称

扩展 supervisor 级虚拟内存架构的标准扩展以"Sv"开头，后跟字母数字名称。扩展 supervisor 级架构的其他标准扩展以"Ss"开头，后跟字母数字名称。名称必须以字母字符结尾。如果最后一个字母是"p"，则倒数第二个字母不能是数字。这些扩展在第二卷中进一步定义。

"sv32"、"sv39"、"sv48"和"sv59"扩展是在禁止扩展名称以数字结尾的规则建立之前定义的。

标准 supervisor 级扩展应列在标准非特权扩展之后，并且与其他多字母扩展一样，必须用下划线与其他多字母扩展分隔。如果列出了多个 supervisor 级扩展，它们应按字母顺序排序。

=== Hypervisor 级指令集扩展名称

扩展 hypervisor 级架构的标准扩展以"Sh"开头。如果列出了多个 hypervisor 级扩展，它们应按字母顺序排序。

注意：许多对 hypervisor 级架构的增强更自然地定义为 supervisor 级扩展，遵循上一节中描述的方案。"Sh"前缀用于少数没有 supervisor 可见效果的 hypervisor 级扩展。

=== 机器级指令集扩展名称

标准机器级指令集扩展以"Sm"开头。

标准机器级扩展应列在标准较低特权扩展之后，并且与其他多字母扩展一样，必须用下划线与其他多字母扩展分隔。如果列出了多个机器级扩展，它们应按字母顺序排序。

=== 非标准扩展名称

非标准扩展通过使用单个"X"后跟字母数字名称来命名。名称必须以字母字符结尾。如果最后一个字母是"p"，则倒数第二个字母不能是数字。例如，"Xhwacha"命名了 Hwacha 向量获取 ISA 扩展。

非标准扩展必须列在所有标准扩展之后，并且与其他多字母扩展一样，必须用下划线与其他多字母扩展分隔。例如，具有非标准扩展 Argle 和 Bargle 的 ISA 可以命名为"RV64IZifencei_Xargle_Xbargle"。

如果列出了多个非标准扩展，它们应按字母顺序排序。与其他多字母扩展一样，它们应与其他多字母扩展用下划线分隔。

=== 版本号

认识到指令集可能会随着时间而扩展或更改，我们在扩展名称后编码扩展版本号。版本号分为主要版本号和次要版本号，用"p"分隔。如果次要版本是"0"，则版本字符串中的"p0"可以省略。为避免歧义，任何扩展名称都不能以数字或以数字开头的"p"结尾。

由于 Packed SIMD 的"P"扩展可能与版本号中的小数点混淆，因此如果它跟在带有版本号的另一个扩展之后，则必须在其前面加上下划线。例如，"rv32i2p2"表示 RV32I 的 2.2 版，而"rv32i2_p2"表示 RV32I 的 2.0 版和 P 扩展的 2.0 版。

主要版本号的更改意味着向后兼容性丢失，而次要版本号的更改必须向后兼容。例如，本手册 1.0 版中定义的原始 64 位标准 ISA 可以完整地写为"RV64I1p0M1p0A1p0F1p0D1p0"，更简洁地写为"RV64I1M1A1F1D1"。

我们在第二个版本中引入了版本编号方案。因此，我们将标准扩展的默认版本定义为当时存在的版本，例如，"RV32I"等效于"RV32I2"。

=== 子集命名约定

<<isanametable>> 总结了标准化的扩展名称。该表还定义了扩展名称在名称字符串中出现的规范顺序，表中从上到下表示名称字符串中从前到后，例如，RV32IMACV 是合法的，而 RV32IMAVC 则不是。

[[isanametable]]
.标准 ISA 扩展名称。
[%autowidth,float="center",align="center",cols="<,^,^",options="header",]
|===
|子集 |名称 |含义

|基本 ISA | |

|整数 |I |

|精简整数 |E |

3+|*标准非特权扩展*

|整数乘法和除法 |M |Zmmul

|原子操作 |A |

|单精度浮点 |F |Zicsr

|双精度浮点 |D |F

|通用 |G |IMAFDZicsr_Zifencei

|四精度浮点 |Q |D

|16 位压缩指令 |C |

|B 扩展 |B |

|打包 SIMD 扩展 |P |

|向量扩展 |V |D

|Hypervisor 扩展 |H |

3+|*其他标准非特权扩展*

|其他标准非特权扩展"abc" |Zabc |

3+|*标准 Supervisor 级扩展*

|Supervisor 级扩展"def" |Ssdef |

3+|*标准 Hypervisor 级扩展*

|Hypervisor 级扩展"ghi" |Shghi |

3+|*标准机器级扩展*

|机器级扩展"jkl" |Smjkl |

3+|*非标准扩展*

|非标准扩展"mno" |Xmno |
|===